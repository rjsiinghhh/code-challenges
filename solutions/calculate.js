function checkParity(parity, bin) {

    // parity is always even or odd
    // the total number of 1-bits in the string
    // parity = even
    // bin = 0101010
    // result = 1

    // bin = 0101
    // result = 0 because there are a even number of 1's in the bin



    if(parity === 'even'){

    }
}