// take two number as the parameters 

// first number being the coeffiencent 

// second being the exponent


//function should multiply the two numbers and then subtract 1 from the exponent

// has to print out an expression like (28x ^7) 


// example (derive 7, 8) 


function derive(a,b) {
return ( a* b) + "x^" + (b -1);
    // expression:  '(multiplied numbers) x ^(exponent - 1)'

    // multiply cooefficient * exponent

    // subtract exponent - 1

    // output the expression

  }